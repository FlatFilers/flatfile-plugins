{
  "name": "@flatfile/plugin-automation-webhook-event-forwarding",
  "version": "0.0.1",
  "description": "A plugin to forward events via webhook for clients to manipulate at their endpoints in a language-agnostic way.",
  "source": "src/index.ts",
  "main": "dist/main.js",
  "module": "dist/module.mjs",
  "types": "dist/types.d.ts",
  "scripts": {
    "build": "parcel build",
    "dev": "parcel watch",
    "check": "tsc ./**/*.ts --noEmit --esModuleInterop",
    "test": "jest ./**/*.spec.ts --config=../../jest.config.js --runInBand"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/FlatFilers/flatfile-plugins.git",
    "directory": "packages/forward-webhook"
  },
  "author": "Morgan Daley",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/FlatFilers/flatfile-plugins/issues"
  },
  "homepage": "https://github.com/FlatFilers/flatfile-plugins#readme",
  "dependencies": {
    "@flatfile/api": "^1.5.37",
    "@flatfile/cross-env-config": "^0.0.3",
    "@flatfile/listener": "^0.3.17",
    "@types/express": "^4.17.21",
    "axios": "^1.6.1"
  }
}
